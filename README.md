To run the project it is necessary to install the deno follows the documentation link : https://deno.land/

After installed run in the terminal ./executar.sh or deno run --allow-env --allow-net app.js